class GeneSearchPanel
    @controller: (args) ->
        return @

    @view: (ctrl) ->


SelectedGenes =
    view: (ctrl, args) ->

SearchSelect =
    controller: (args) ->
        
    view: (ctrl, args) ->

CategorySelection =
    controller: (args) ->
    view: (ctrl, args) ->
