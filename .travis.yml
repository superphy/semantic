language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install --upgrade pip
  - pip install -r requirements/requirements.txt
# command to run tests
before_script: xvfb-run -a
script: nosetests