language: python

python:
  - "2.7"

addons:
  firefox: "41.0"
# command to install dependencies

install:
  - venv/bin/pip install --upgrade pip
  - venv/bin/pip install -r venv/requirements.txt
  
# command to run tests
script: xvfb-run -a nosetests && xvfb-run -a jasmine-ci